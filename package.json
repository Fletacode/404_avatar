{
  "name": "hedra-avatar-app",
  "version": "0.1.6",
  "scripts": {
    "start-app": "cd frontend && pnpm install && pnpm dev",
    "start-agent": "cd backend && pip install -r requirements.txt && python agent_worker.py start",
    "start-backend": "cd backend-nestjs && npm install && npm run start:dev",
    "build-backend": "cd backend-nestjs && npm install && npm run build",
    "start-backend-prod": "cd backend-nestjs && npm run start:prod"
  }
}
